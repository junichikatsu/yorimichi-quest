/**
 * ゲーム画面らしく見せるための、地図の塗り分け。
 *
 * 元のスタイル（streets-v12）の色は現実の地図として最適化されていて、彩度も明度も近い色が多い。
 * それを機械的にパレットへ丸めると、灰色と青緑ばかりの「潰れた地図」になる。
 * 実際そうなった。近い色を探すのではなく、**地物ごとに色を決め打ちする**必要がある。
 *
 * 選んでいるのはすべてファミコンのパレットにある色。最後の丸めでも変化しない。
 * 図と地がはっきり分かれるよう、明度差の大きい組み合わせにしている。
 */
export const GAME_COLORS = {
  /** 地面。淡い砂色（$37） */
  land: '#fce0a8',
  /** 建物。地面より暗い金茶（$18）。都心では面積が広いので、地の模様として効く */
  building: '#ac7c00',
  /** 緑地（$1A） */
  green: '#00a800',
  /** 水面（$12） */
  water: '#0058f8',
  /** 幹線道路。いちばん明るい琥珀（$28） */
  roadMajor: '#f8b800',
  /** 生活道路。白（$30） */
  roadMinor: '#fcfcfc',
} as const

/**
 * 未踏エリアを覆う霧の色（8bit 風表示）。
 *
 * **半透明では重ねない。** 実機に半透明は無く、市松模様で薄く見せていた。
 * ここでも同じことをする。1 ドットおきにこの色で塗り、残りは地図の色をそのまま残す。
 *
 * 半透明で重ねると全部の色が濁る。実際、地面の砂色が灰色に、水の青が紫に転んでいた。
 * せっかく地物ごとに色を決めても、上から一律に混ぜてしまえば台無しになる。
 */
export const RETRO_FOG_COLOR = { r: 0, g: 64, b: 88 } as const
